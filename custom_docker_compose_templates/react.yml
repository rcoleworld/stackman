react-frontend:
    container_name: react-frontend
    build:
        context: ./frontend
        dockerfile: Dockerfile
    volumes:
        - './frontend:/usr/src/app/frontend'
        - '/app/node_modules'
    ports:
        - '3000:3000'
    environment:
        - NODE_ENV=development